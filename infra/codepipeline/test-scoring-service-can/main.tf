terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# CodePipeline for test-scoring-service → can
#
# This file was generated by the Backstage java-service / python-service
# scaffolder template. It instantiates the shared `codepipeline` Terraform
# module. Update inputs here, not in the module.
#
# To apply: this is normally picked up by the IaC repo's Terraform CI on merge.

module "test_scoring_service_can" {
  source = "../../../modules/codepipeline"

  pipeline_name      = "test-scoring-service-can"
  service_name       = "test-scoring-service"
  market             = "can"
  group              = "ai/insights"

  source_repo        = "priyankaajbhagatt-collab/test-scoring-service"
  source_branch      = "main"

  build_image        = "python"
  buildspec_path     = "buildspec.yml"

  # CD stage notifies ArgoCD via webhook — app naming convention matches
  # what CompositeLiveProvider.linkArgoApps() expects.
  argocd_app_name    = "test-scoring-service-can"

  tags = {
    Service     = "test-scoring-service"
    Market      = "can"
    Group       = "ai/insights"
    ManagedBy   = "backstage-scaffolder"
  }
}

output "pipeline_arn" {
  value = module.test_scoring_service_can.pipeline_arn
}
