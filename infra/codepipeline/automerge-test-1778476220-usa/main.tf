terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# CodePipeline for automerge-test-1778476220 → usa
#
# This file was generated by the Backstage java-service / python-service
# scaffolder template. It instantiates the shared `codepipeline` Terraform
# module. Update inputs here, not in the module.
#
# To apply: this is normally picked up by the IaC repo's Terraform CI on merge.

module "automerge_test_1778476220_usa" {
  source = "../../../modules/codepipeline"

  pipeline_name      = "automerge-test-1778476220-usa"
  service_name       = "automerge-test-1778476220"
  market             = "usa"
  group              = "commerce/orderas"

  source_repo        = "priyankaajbhagatt-collab/automerge-test-1778476220"
  source_branch      = "main"

  build_image        = "java"
  buildspec_path     = "buildspec.yml"

  # CD stage notifies ArgoCD via webhook — app naming convention matches
  # what CompositeLiveProvider.linkArgoApps() expects.
  argocd_app_name    = "automerge-test-1778476220-usa"

  tags = {
    Service     = "automerge-test-1778476220"
    Market      = "usa"
    Group       = "commerce/orderas"
    ManagedBy   = "backstage-scaffolder"
  }
}

output "pipeline_arn" {
  value = module.automerge_test_1778476220_usa.pipeline_arn
}
